# List of this module's dependencies (no external dependencies)
manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  # projects:
  #   - name: repository-name
  #     remote: remote-name
  #     revision: branch-or-tag-name
